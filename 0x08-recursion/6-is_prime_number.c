#include "main.h"
/**
 * is_prime_number - Prints prime number
 * @n: Integer to be checked
 * Return: 1 if integer is prime number,
 * or 0 if integer is not
 */
int is_prime_number(int n)
{
	if (n / 1 == n && n / n == 1)
	{
		return (1);
	}
	else if (n <= 1)
	{
		return (0)
	}
	else
	{
		return (0);
	}
}
