#include "main.h"

/**
 * _abs - Prints absolute value of an integer
 * @j: integer to be checked
 * Return: absolute value of the integer
 */
int _abs(int j)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
